box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: jspm install
        code: |
          node_modules/.bin/jspm install
    - script:
        name: karma
        code: |
          node_modules/karma/bin/karma start
