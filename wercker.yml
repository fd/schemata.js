box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: jspm install
        code: |
          jspm install
    - script:
        name: karma
        code: |
          karma start
